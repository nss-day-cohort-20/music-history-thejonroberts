	<!-- ARTIST DROP -->
	<legend>Artist:</legend>
	<select id="artist-select" class="fullwidth">
		{{#each songs}}
			<option>{{artist}}</option>
		{{/each}}
	</select>
	<!-- ALBUM DROP -->
	<legend>Album:</legend>
	<select id="album-select" class="fullwidth">
		{{#each songs}}
			<option>{{album}}</option>
		{{/each}}
	</select>
